{
  "name": "url-shortener-lambda",
  "version": "1.0.0",
  "description": "Lambda function for URL Shortener API",
  "main": "index.js",
  "scripts": {
    "build": "bash build.sh",
    "dev": "sam local start-api"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.400.0",
    "@aws-sdk/lib-dynamodb": "^3.400.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
